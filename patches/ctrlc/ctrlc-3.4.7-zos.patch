diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.cargo-ok /tmp/ctrlc-zos/.cargo-ok
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.cargo-ok	2026-07-14 13:11:29.544012271 -0400
+++ /tmp/ctrlc-zos/.cargo-ok	1969-12-31 19:00:00.000000000 -0500
@@ -1 +0,0 @@
-{"v":1}
\ No newline at end of file
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/Cargo.toml /tmp/ctrlc-zos/Cargo.toml
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/Cargo.toml	1969-12-31 19:00:01.000000000 -0500
+++ /tmp/ctrlc-zos/Cargo.toml	2026-08-11 15:19:57.641913605 -0400
@@ -13,7 +13,7 @@
 edition = "2021"
 rust-version = "1.69.0"
 name = "ctrlc"
-version = "3.4.7"
+version = "3.5.2"
 authors = ["Antti Keränen <detegr@gmail.com>"]
 build = false
 exclude = [
@@ -69,16 +69,16 @@
 [dev-dependencies.signal-hook]
 version = "0.3"
 
+[target.'cfg(target_vendor = "apple")'.dependencies.dispatch2]
+version = "0.3"
+
 [target."cfg(unix)".dependencies.nix]
-version = "0.30"
-features = [
-    "fs",
-    "signal",
-]
+version = "0.31"
+features = ["signal"]
 default-features = false
 
 [target."cfg(windows)".dependencies.windows-sys]
-version = "0.59"
+version = "0.61"
 features = [
     "Win32_Foundation",
     "Win32_System_Threading",
@@ -87,7 +87,7 @@
 ]
 
 [target."cfg(windows)".dev-dependencies.windows-sys]
-version = "0.59"
+version = "0.61"
 features = [
     "Win32_Storage_FileSystem",
     "Win32_Foundation",
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.cargo_vcs_info.json /tmp/ctrlc-zos/.cargo_vcs_info.json
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.cargo_vcs_info.json	1969-12-31 19:00:01.000000000 -0500
+++ /tmp/ctrlc-zos/.cargo_vcs_info.json	1969-12-31 19:00:00.000000000 -0500
@@ -1,6 +0,0 @@
-{
-  "git": {
-    "sha1": "ac79af3262199bce508257771b61608df4db22c4"
-  },
-  "path_in_vcs": ""
-}
\ No newline at end of file
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.github/workflows/ci.yml /tmp/ctrlc-zos/.github/workflows/ci.yml
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.github/workflows/ci.yml	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/.github/workflows/ci.yml	1969-12-31 19:00:00.000000000 -0500
@@ -1,45 +0,0 @@
-name: CI
-
-on:
-  push:
-    branches: [master]
-  pull_request:
-    branches: [master]
-
-jobs:
-  test:
-    strategy:
-      fail-fast: false
-      matrix:
-        os: [ubuntu-latest, macos-latest, windows-latest]
-        rust: [stable, beta, nightly]
-        include:
-          - os: windows-latest
-            rust: stable-x86_64-pc-windows-gnu
-          - os: windows-latest
-            rust: nightly-x86_64-pc-windows-gnu
-          - os: windows-latest
-            rust: stable-i686-pc-windows-msvc
-          - os: windows-latest
-            rust: nightly-i686-pc-windows-msvc
-          - os: windows-latest
-            rust: stable-i686-pc-windows-gnu
-          - os: windows-latest
-            rust: nightly-i686-pc-windows-gnu
-    runs-on: ${{ matrix.os }}
-    steps:
-      - uses: actions/checkout@v4
-      - uses: dtolnay/rust-toolchain@stable
-        with:
-          toolchain: ${{ matrix.rust }}
-      - run: cargo build
-      - run: cargo build --features termination
-      - run: cargo test
-      - run: cargo test --features termination
-
-  fmt:
-    runs-on: ubuntu-latest
-    steps:
-      - uses: actions/checkout@v4
-      - uses: dtolnay/rust-toolchain@stable
-      - run: cargo fmt --check
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.gitignore /tmp/ctrlc-zos/.gitignore
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/.gitignore	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/.gitignore	1969-12-31 19:00:00.000000000 -0500
@@ -1,2 +0,0 @@
-target
-Cargo.lock
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/README.md /tmp/ctrlc-zos/README.md
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/README.md	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/README.md	2026-08-11 15:19:57.641913605 -0400
@@ -9,7 +9,7 @@
 
 ```toml
 [dependencies]
-ctrlc = "3.4"
+ctrlc = "3.5"
 ```
 
 then, in `main.rs`
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/lib.rs /tmp/ctrlc-zos/src/lib.rs
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/lib.rs	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/src/lib.rs	2026-08-11 15:19:57.642671489 -0400
@@ -42,7 +42,7 @@
 //! ```
 //!
 //! # Handling SIGTERM and SIGHUP
-//! Handling of `SIGTERM and SIGHUP` can be enabled with `termination` feature. If this is enabled,
+//! Handling of `SIGTERM` and `SIGHUP` can be enabled with `termination` feature. If this is enabled,
 //! the handler specified by `set_handler()` will be executed for `SIGINT`, `SIGTERM` and `SIGHUP`.
 //!
 
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/platform/unix/mod.rs /tmp/ctrlc-zos/src/platform/unix/mod.rs
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/platform/unix/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/src/platform/unix/mod.rs	2026-08-11 15:20:15.969943750 -0400
@@ -8,63 +8,86 @@
 // according to those terms.
 
 use crate::error::Error as CtrlcError;
-use nix::unistd;
-use std::os::fd::BorrowedFd;
-use std::os::fd::IntoRawFd;
-use std::os::unix::io::RawFd;
 
-static mut PIPE: (RawFd, RawFd) = (-1, -1);
+#[cfg(not(any(target_vendor = "apple", target_os = "zos")))]
+#[allow(static_mut_refs)] // rust-version = "1.69.0"
+mod implementation {
+    static mut SEMAPHORE: nix::libc::sem_t = unsafe { std::mem::zeroed() };
+    const SEM_THREAD_SHARED: nix::libc::c_int = 0;
 
-/// Platform specific error type
-pub type Error = nix::Error;
+    pub unsafe fn sem_init() {
+        nix::libc::sem_init(&mut SEMAPHORE as *mut _, SEM_THREAD_SHARED, 0);
+    }
 
-/// Platform specific signal type
-pub type Signal = nix::sys::signal::Signal;
+    pub unsafe fn sem_post() {
+        // No errors apply. EOVERFLOW is hypothetically possible but it's equivalent to a success for our oneshot use-case.
+        let _ = nix::libc::sem_post(&mut SEMAPHORE as *mut _);
+    }
 
-extern "C" fn os_handler(_: nix::libc::c_int) {
-    // Assuming this always succeeds. Can't really handle errors in any meaningful way.
-    unsafe {
-        let fd = BorrowedFd::borrow_raw(PIPE.1);
-        let _ = unistd::write(fd, &[0u8]);
+    pub unsafe fn sem_wait_forever() {
+        // The only realistic error is EINTR, which is restartable.
+        while nix::libc::sem_wait(&mut SEMAPHORE as *mut _) == -1 {}
     }
 }
 
-// pipe2(2) is not available on macOS, iOS, AIX, Haiku, etc., so we need to use pipe(2) and fcntl(2)
-#[inline]
-#[cfg(any(
-    target_vendor = "apple",
-    target_os = "haiku",
-    target_os = "aix",
-    target_os = "nto",
-))]
-fn pipe2(flags: nix::fcntl::OFlag) -> nix::Result<(RawFd, RawFd)> {
-    use nix::fcntl::{fcntl, FcntlArg, FdFlag, OFlag};
 
-    let pipe = unistd::pipe()?;
+#[cfg(target_os = "zos")]
+mod implementation {
+    static mut PIPE_RD: nix::libc::c_int = -1;
+    static mut PIPE_WR: nix::libc::c_int = -1;
+
+    pub unsafe fn sem_init() {
+        let mut fds = [0i32; 2];
+        nix::libc::pipe(fds.as_mut_ptr());
+        PIPE_RD = fds[0];
+        PIPE_WR = fds[1];
+    }
+
+    pub unsafe fn sem_post() {
+        let buf = [1u8; 1];
+        let _ = nix::libc::write(PIPE_WR, buf.as_ptr() as *const _, 1);
+    }
+
+    pub unsafe fn sem_wait_forever() {
+        loop {
+            let mut buf = [0u8; 1];
+            let r = nix::libc::read(PIPE_RD, buf.as_mut_ptr() as *mut _, 1);
+            if r >= 0 { break; }
+            if *nix::libc::__errno() != nix::libc::EINTR { break; }
+        }
+    }
+}
+#[cfg(target_vendor = "apple")]
+mod implementation {
+    use dispatch2::{DispatchRetained, DispatchSemaphore, DispatchTime};
+
+    static mut SEMAPHORE: Option<DispatchRetained<DispatchSemaphore>> = None;
 
-    if flags.contains(OFlag::O_CLOEXEC) {
-        fcntl(&pipe.0, FcntlArg::F_SETFD(FdFlag::FD_CLOEXEC))?;
-        fcntl(&pipe.1, FcntlArg::F_SETFD(FdFlag::FD_CLOEXEC))?;
+    pub unsafe fn sem_init() {
+        SEMAPHORE = Some(DispatchSemaphore::new(0));
     }
 
-    if flags.contains(OFlag::O_NONBLOCK) {
-        fcntl(&pipe.0, FcntlArg::F_SETFL(OFlag::O_NONBLOCK))?;
-        fcntl(&pipe.1, FcntlArg::F_SETFL(OFlag::O_NONBLOCK))?;
+    #[allow(static_mut_refs)]
+    pub unsafe fn sem_post() {
+        SEMAPHORE.as_deref().unwrap().signal();
     }
 
-    Ok((pipe.0.into_raw_fd(), pipe.1.into_raw_fd()))
+    #[allow(static_mut_refs)]
+    pub unsafe fn sem_wait_forever() {
+        SEMAPHORE.as_deref().unwrap().wait(DispatchTime::FOREVER);
+    }
 }
 
-#[inline]
-#[cfg(not(any(
-    target_vendor = "apple",
-    target_os = "haiku",
-    target_os = "aix",
-    target_os = "nto",
-)))]
-fn pipe2(flags: nix::fcntl::OFlag) -> nix::Result<(RawFd, RawFd)> {
-    let pipe = unistd::pipe2(flags)?;
-    Ok((pipe.0.into_raw_fd(), pipe.1.into_raw_fd()))
+/// Platform specific error type
+pub type Error = nix::Error;
+
+/// Platform specific signal type
+pub type Signal = nix::sys::signal::Signal;
+
+extern "C" fn os_handler(_: nix::libc::c_int) {
+    unsafe {
+        implementation::sem_post();
+    }
 }
 
 /// Register os signal handler.
@@ -77,26 +100,9 @@
 ///
 #[inline]
 pub unsafe fn init_os_handler(overwrite: bool) -> Result<(), Error> {
-    use nix::fcntl;
     use nix::sys::signal;
 
-    PIPE = pipe2(fcntl::OFlag::O_CLOEXEC)?;
-
-    let close_pipe = |e: nix::Error| -> Error {
-        // Try to close the pipes. close() should not fail,
-        // but if it does, there isn't much we can do
-        let _ = unistd::close(PIPE.1);
-        let _ = unistd::close(PIPE.0);
-        e
-    };
-
-    // Make sure we never block on write in the os handler.
-    if let Err(e) = fcntl::fcntl(
-        BorrowedFd::borrow_raw(PIPE.1),
-        fcntl::FcntlArg::F_SETFL(fcntl::OFlag::O_NONBLOCK),
-    ) {
-        return Err(close_pipe(e));
-    }
+    implementation::sem_init();
 
     let handler = signal::SigHandler::Handler(os_handler);
     #[cfg(not(target_os = "nto"))]
@@ -110,13 +116,10 @@
     let new_action =
         signal::SigAction::new(handler, signal::SaFlags::empty(), signal::SigSet::empty());
 
-    let sigint_old = match signal::sigaction(signal::Signal::SIGINT, &new_action) {
-        Ok(old) => old,
-        Err(e) => return Err(close_pipe(e)),
-    };
-    if !overwrite && sigint_old.handler() != signal::SigHandler::SigDfl {
+    let sigint_old = signal::sigaction(signal::Signal::SIGINT, &new_action)?;
+    if !overwrite && !matches!(sigint_old.handler(), signal::SigHandler::SigDfl) {
         signal::sigaction(signal::Signal::SIGINT, &sigint_old).unwrap();
-        return Err(close_pipe(nix::Error::EEXIST));
+        return Err(nix::Error::EEXIST);
     }
 
     #[cfg(feature = "termination")]
@@ -125,27 +128,27 @@
             Ok(old) => old,
             Err(e) => {
                 signal::sigaction(signal::Signal::SIGINT, &sigint_old).unwrap();
-                return Err(close_pipe(e));
+                return Err(e);
             }
         };
-        if !overwrite && sigterm_old.handler() != signal::SigHandler::SigDfl {
+        if !overwrite && !matches!(sigterm_old.handler(), signal::SigHandler::SigDfl) {
             signal::sigaction(signal::Signal::SIGINT, &sigint_old).unwrap();
             signal::sigaction(signal::Signal::SIGTERM, &sigterm_old).unwrap();
-            return Err(close_pipe(nix::Error::EEXIST));
+            return Err(nix::Error::EEXIST);
         }
         let sighup_old = match signal::sigaction(signal::Signal::SIGHUP, &new_action) {
             Ok(old) => old,
             Err(e) => {
                 signal::sigaction(signal::Signal::SIGINT, &sigint_old).unwrap();
                 signal::sigaction(signal::Signal::SIGTERM, &sigterm_old).unwrap();
-                return Err(close_pipe(e));
+                return Err(e);
             }
         };
-        if !overwrite && sighup_old.handler() != signal::SigHandler::SigDfl {
+        if !overwrite && !matches!(sighup_old.handler(), signal::SigHandler::SigDfl) {
             signal::sigaction(signal::Signal::SIGINT, &sigint_old).unwrap();
             signal::sigaction(signal::Signal::SIGTERM, &sigterm_old).unwrap();
             signal::sigaction(signal::Signal::SIGHUP, &sighup_old).unwrap();
-            return Err(close_pipe(nix::Error::EEXIST));
+            return Err(nix::Error::EEXIST);
         }
     }
 
@@ -157,24 +160,10 @@
 /// Must be called after calling [`init_os_handler()`](fn.init_os_handler.html).
 ///
 /// # Errors
-/// Will return an error if a system error occurred.
+/// None.
 ///
 #[inline]
 pub unsafe fn block_ctrl_c() -> Result<(), CtrlcError> {
-    use std::io;
-    let mut buf = [0u8];
-
-    // TODO: Can we safely convert the pipe fd into a std::io::Read
-    // with std::os::unix::io::FromRawFd, this would handle EINTR
-    // and everything for us.
-    loop {
-        match unistd::read(BorrowedFd::borrow_raw(PIPE.0), &mut buf[..]) {
-            Ok(1) => break,
-            Ok(_) => return Err(CtrlcError::System(io::ErrorKind::UnexpectedEof.into())),
-            Err(nix::errno::Errno::EINTR) => {}
-            Err(e) => return Err(e.into()),
-        }
-    }
-
+    implementation::sem_wait_forever();
     Ok(())
 }
diff -ruN '--exclude=*.orig' '--exclude=Cargo.lock' '--exclude=.git' /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/platform/windows/mod.rs /tmp/ctrlc-zos/src/platform/windows/mod.rs
--- /home/itodorov/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ctrlc-3.4.7/src/platform/windows/mod.rs	2006-07-23 21:21:28.000000000 -0400
+++ /tmp/ctrlc-zos/src/platform/windows/mod.rs	2026-08-11 15:19:57.643025084 -0400
@@ -9,7 +9,8 @@
 
 use std::io;
 use std::ptr;
-use windows_sys::Win32::Foundation::{CloseHandle, BOOL, HANDLE, WAIT_FAILED, WAIT_OBJECT_0};
+use windows_sys::core::BOOL;
+use windows_sys::Win32::Foundation::{CloseHandle, HANDLE, WAIT_FAILED, WAIT_OBJECT_0};
 use windows_sys::Win32::System::Console::SetConsoleCtrlHandler;
 use windows_sys::Win32::System::Threading::{
     CreateSemaphoreA, ReleaseSemaphore, WaitForSingleObject, INFINITE,
